<?xml version="1.0"?>
<fragment version="1">

  <install>
    <!-- Unzip the contents of our web application into the server -->
    <unzip from="${bundle.fileName}" to="/">
      <include>timeoff/**</include>
    </unzip>
  </install>

</fragment>

